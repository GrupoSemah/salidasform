[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "adduser --disabled-password --gecos '' --uid 1001 appuser 2>/dev/null; exec su appuser -s /bin/sh -c 'npx serve dist -l ${PORT:-4321}'"
